return {
   'echasnovski/mini.indentscope',
   opts = {
      -- Draw options
      draw = {
         -- Delay (in ms) between event and start of drawing scope indicator
         delay = 20,
      },
      symbol = '',
   }
}
